/* ============================================
   Tereré Mix — Vercel Serverless Entry Point
   A Vercel usa este arquivo como handler.
   Ele apenas re-exporta o app Express configurado.
   ============================================ */

module.exports = require('../server/app');
