/* ============================================
   Tereré Mix — Database Model
   Re-exporta a instância e inicializador do
   config/database.js para manter backward
   compatibility com todos os controllers.
   ============================================ */

module.exports = require('../config/database');
